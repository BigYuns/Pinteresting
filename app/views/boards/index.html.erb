<h2>Index of the Boards </h2>
<p>This is current user's email:
<%=current_user.email%></p>

<%counter=0%>
<br>Name of the Board</br>
<%@boards.each do |board|%>
	<%counter+=1%>
	<br><%=board.name%></br>
	<%=link_to 'Show each Board', user_board_path(current_user, board)%>
	<%=link_to 'Edit Each Board', edit_user_board_path(current_user,board)%>
	<%=link_to 'Create New Board', new_user_board_path(current_user)%>

	<%end%>

<p>
The number of the Board:
<%=counter%>
</p>