<<<<<<< HEAD
<h2>Welcome to the show page of the <%=@board.name%></h2>

<%pin=0%>

<p>These are your pins in the Board! </p>

<%@pins.each do |pin|%>
	<br><%=pin.title%></br>
	<%= image_tag(pin.url, width: "30%", height: "30%") %>
	<%=link_to 'EditPin', edit_user_board_pin_path(:user_id,:board_id)%>
	<%=link_to 'DeletePin', user_board_pin_path(:user_id,:board_id), method: :delete, data: {confirm:"Do you really want to delete this Pin?"} %>
<%end%>

<br><%=link_to 'AddPin', new_user_board_pin_path(:user_id,:board_id)%></br>
=======

<h1><%=@board.name%></h1>
<%pinnumber=0%>



<%@pins.each do |pin|%>
	<br><%=pin.title%></br>
	<br><%= link_to image_tag(pin.url, width: "30%", height: "30%"), user_board_pin_path(current_user,@board.id,pin.id) %></br>

	<% if @board.user==current_user %>
		<%=link_to 'EditPin', edit_user_board_pin_path(current_user,@board.id, pin.id)%>,
		<%=link_to 'DeletePin', user_board_pin_path(current_user,@board.id, pin.id), method: :delete, data: {confirm:"Do you really want to delete this Pin?"} %><br>
	<%end%>
<%end%>

<% if @board.user==current_user %>
	<%=link_to 'Create a new Pin', new_user_board_pin_path(current_user,@board.id)%>
<%end%>
	<br><%=link_to 'Boards List', user_boards_path(current_user)%></br>

>>>>>>> 236044ed06d85d2750b48bd1e543b20e7ecd009e
